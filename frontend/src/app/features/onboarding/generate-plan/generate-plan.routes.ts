import { Routes } from '@angular/router';

import { GeneratePlanPage } from './generate-plan.page';

export const generatePlanRoutes: Routes = [
  {
    path: '',
    component: GeneratePlanPage,
  },
];
