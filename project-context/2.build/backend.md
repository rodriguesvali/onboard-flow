# Backend Build Notes — OnboardFlow AI

Generated by: `@backend.eng`  
Phase: Build  
Date: 2026-05-31  
Status: Approved by Agentic Architect

## 1. Scope Implemented

Implemented the initial Python backend service for the OnboardFlow AI MVP.

Included:

- `backend/` Python package managed with `uv`.
- FastAPI service with approved routes:
  - `POST /api/onboarding/generate`
  - `GET /api/onboarding/runs/{runId}`
  - `POST /api/onboarding/runs/{runId}/refine`
- Pydantic v2 schemas for employee input, run status, final plan, refinement, validation, specialist output, and action history.
- DDD/Hexagonal-oriented package structure with domain/application code separated from adapters.
- Local JSON catalogs for documents, IT access/equipment, training, and pt-BR communication templates.
- Deterministic Flow/Crew boundary adapter for local MVP execution and tests.
- CrewAI `agents.yaml` and `tasks.yaml` configs for the bounded specialist crew.
- SQLite persistence with SQLAlchemy.
- Alembic migration `0001_create_onboarding_runs`.
- Markdown rendering derived from validated JSON.
- Mock HR user action-history attribution.
- Model-profile settings for `default`, `reasoning`, `efficient`, `creative`, `tool_calling`, and `refinement`, all defaulting to `gemini-3.5-flash`.
- Tests for generation, run retrieval, invalid input, refinement, and frontend-compatible camelCase input.

Excluded:

- Real Gemini provider calls during tests.
- Real HRIS, ITSM, IAM, LMS, email, chat, payroll, SSO/RBAC, analytics, or external provisioning integrations.
- Backend-persisted approval workflow.
- Production deployment hardening.

## 2. Tooling Validation

Mandatory documentation/tooling was checked before implementation:

- Context7 MCP Server was available.
- FastAPI docs were retrieved for route definitions, Pydantic request/response models, dependency injection, CORS, and TestClient guidance.
- CrewAI docs were retrieved for Flow `@start`/`@listen`, `CrewBase`, YAML config, and `Process.sequential`.
- Alembic docs were retrieved for SQLAlchemy metadata and SQLite-compatible migration setup.
- Context7 library resolution was also checked for Pydantic, SQLAlchemy, Alembic, CrewAI, FastAPI, and Google Gen AI Python SDK.

Local version checks:

- Python: `3.12.11`.
- `uv`: available at `/usr/local/bin/uv`.

Build-preparation dependency:

- `project-context/2.build/setup.md` is not present yet.

## 3. Implementation Decisions

- Kept the public API compatible with the approved frontend mock service: generation returns `runId` plus `running`, and run retrieval returns `RunStatusResponse`.
- Implemented deterministic local orchestration so tests do not require live LLM or provider credentials.
- Preserved CrewAI integration boundaries through config files and the Flow/Crew adapter location.
- Treated JSON as the primary plan contract and Markdown as a rendered view.
- Used SQLite persistence and Alembic from the first backend slice instead of file-only run output.
- Kept approval session-local in the frontend; backend refinement is implemented, but backend approval persistence is deferred.
- Did not use `create_all` for runtime database setup; Alembic migration is the runtime schema path.

## 4. Validation Evidence

Commands run from `backend/`:

```text
uv sync --all-extras --dev
uv run alembic upgrade head
uv run pytest
```

Results:

- Dependency sync passed after constraining `pydantic-settings` to CrewAI-compatible `>=2.10.1,<2.11`.
- Alembic migration passed against local SQLite.
- Tests passed: 2 files, 4 tests.

## 5. Files Added Or Changed

Key backend files:

- `backend/pyproject.toml`
- `backend/alembic.ini`
- `backend/src/onboardflow/api/main.py`
- `backend/src/onboardflow/application/services.py`
- `backend/src/onboardflow/domain/models.py`
- `backend/src/onboardflow/adapters/`
- `backend/src/onboardflow/alembic/`
- `backend/src/onboardflow/catalogs/`
- `backend/src/onboardflow/crews/onboarding_analysis_crew/config/`
- `backend/tests/`

Build artifacts:

- `project-context/2.build/backend-implementation-plan.md`
- `project-context/2.build/backend.md`

## 6. Review Notes

Agentic Architect approved this backend implementation gate on 2026-05-31.

Review focus:

- Deterministic Flow/Crew adapter is accepted for the first backend slice.
- Backend-persisted approval remains deferred.
- Next step remains subject to Agentic Architect direction: Angular-to-FastAPI integration, live CrewAI/Gemini execution, or backend hardening.


## 7. CrewAI Live Execution Update

Date: 2026-05-31
Status: Approved by Agentic Architect

Implemented after Agentic Architect direction to proceed with real agent execution. Approved on 2026-05-31.

Included:

- Added `LiveCrewAIOnboardingFlow` as a real CrewAI adapter behind `ONBOARDFLOW_FLOW_MODE=crewai`.
- Preserved `DeterministicOnboardingFlow` as the default mode for local tests and provider-free demos.
- Loaded approved `agents.yaml` and `tasks.yaml` into CrewAI `Agent`, `Task`, and `Crew` objects with `Process.sequential`.
- Configured per-agent model profiles through backend settings and CrewAI `LLM`.
- Passed employee input, validation output, and local catalog data into CrewAI tasks.
- Required structured `SpecialistOutput` JSON from tasks, including optional communication drafts.
- Kept final plan normalization inside the backend Flow so CrewAI output cannot bypass schema validation, catalog-backed safeguards, or human-review boundaries.
- Added failure handling so CrewAI/runtime errors persist an auditable `error` run instead of surfacing as an unhandled API exception.
- Added environment configuration for flow mode, CrewAI verbosity, CrewAI temperature, and Gemini credentials.

Runtime configuration:

```text
ONBOARDFLOW_FLOW_MODE=deterministic  # default
ONBOARDFLOW_FLOW_MODE=crewai         # enables real CrewAI execution
CREWAI_VERBOSE=false
CREWAI_TEMPERATURE=0.2
GEMINI_API_KEY=<required for live Gemini calls>
```

Validation evidence after update:

```text
uv sync --all-extras --dev
PYTHONPATH=src ONBOARDFLOW_FLOW_MODE=crewai uv run python - <<'PY' ... # adapter construction smoke test
uv run python -m compileall src tests
uv run pytest
```

Results:

- CrewAI extras synchronized successfully with `crewai==1.14.6` and `google-genai==1.65.0` after adding the `crewai[google-genai]` optional dependency.
- CrewAI mode adapter construction smoke test returned `LiveCrewAIOnboardingFlow` without executing provider calls.
- Compileall passed for `src` and `tests`.
- Tests passed: 3 files, 8 tests.
- Added a fake-Crew unit test covering `LiveCrewAIOnboardingFlow.execute` without provider calls.
- Existing warning remains from Starlette/FastAPI TestClient requesting future `httpx2`; unrelated to CrewAI execution.

Known boundaries:

- Live Gemini provider calls were executed after `backend/.env` provided `GEMINI_API_KEY` and `ONBOARDFLOW_FLOW_MODE=crewai`.
- CrewAI Knowledge is still not enabled; it remains a candidate enhancement once real policies/catalog documents need retrieval.
- Final plan generation still uses deterministic Flow normalization to preserve the public API contract and MVP review boundary.


Live provider validation on 2026-05-31:

- First live attempt reached CrewAI execution but failed before provider call because native Gemini support required the `crewai[google-genai]` extra.
- Added the `google-genai` CrewAI extra and synchronized dependencies.
- Second live attempt completed successfully with run `run_7e47a37304f24fb2`.
- Result status was `done` with no `errorMessage`.
- Five specialist task actions were persisted: HR Intake, Compliance, IT Provisioning, Training, and Communication.
- Normalized final plan preserved deterministic catalog sections: 2 required documents, 3 IT checklist items, 3 training items, and 4 communication drafts.
- Adjusted Flow normalization so CrewAI can enrich risks, pending actions, summaries, and drafts, while document/IT/training section membership remains catalog-derived and deterministic.

## 8. Post-QA Backend Log Instrumentation

Date: 2026-06-01
Status: Approved by Agentic Architect

Implemented after Agentic Architect direction to instrument backend processing with log messages only, without changing UI behavior or public API contracts.

Documentation/tooling checked before implementation:

- Context7 CrewAI documentation for current Event Listener and event bus usage.

Included:

- Startup log prints the configured backend flow mode through the visible Uvicorn server logger as `onboardflow_backend_started flow_mode=<deterministic|crewai>`.
- Application-level logs for onboarding generation start/completion, input validation start/completion, Flow execution start/completion/failure, plan normalization, refinement start/completion, and refinement fallback.
- Deterministic-mode agent-operation logs for document checklist, IT checklist, training path, stakeholder message draft generation, and refinement.
- CrewAI-mode Event Listener registration using CrewAI's event bus for Crew kickoff start/completion/failure, Task start/completion/failure, and Agent execution start/completion/failure events.
- Context variables around CrewAI kickoff to include the current `run_id` and operation type (`generation` or `refinement`) in CrewAI event logs.
- Tests using `caplog` to verify startup mode, deterministic generation, and refinement operation logs.
- Manual Uvicorn smoke confirmed the startup line appears in the terminal; the current local `.env` reported `flow_mode=crewai`.

Validation evidence:

```text
uv run pytest
uv run python -m compileall src tests
```

Results:

- Backend tests passed: 3 files, 12 tests.
- Compileall passed for `src` and `tests`.
- Short Uvicorn startup smoke on port 8010 printed `onboardflow_backend_started flow_mode=crewai`.
- Existing warning remains from Starlette/FastAPI TestClient requesting future `httpx2`; unrelated to instrumentation.
- Agentic Architect approved the backend log instrumentation refinement.

## 9. Post-QA CrewAI AMP Observability

Date: 2026-06-01
Status: Pending Agentic Architect review

Implemented after Agentic Architect direction to continue the Build phase and add observability through CrewAI AMP.

Documentation/tooling checked before implementation:

- Context7 CrewAI documentation for built-in tracing and CrewAI AMP setup.
- Current CrewAI documentation states that AMP tracing can be enabled with `tracing=True` on a `Crew` instance, and that this code flag has higher priority than environment variables or local user preferences.
- Current CrewAI CLI exposes trace collection management through `crewai traces enable` and `crewai traces status`, in addition to `crewai login`.

Included:

- Added backend setting `crewai_amp_tracing_enabled`, exposed through `ONBOARDFLOW_CREWAI_AMP_TRACING` and compatible with `CREWAI_TRACING_ENABLED`.
- Default remains `false` so deterministic tests, provider-free demos, and local development do not publish traces accidentally.
- Passed `tracing=<configured flag>` to both live CrewAI generation and live CrewAI refinement `Crew` instances.
- Startup log now prints `onboardflow_backend_started flow_mode=<mode> crewai_amp_tracing=<true|false>`.
- Updated backend README with the AMP login, trace-collection enable/status, and runtime commands.
- Added tests proving CrewAI generation receives `tracing=True` when explicitly enabled and refinement receives `tracing=False` by default.

Runtime configuration:

```text
ONBOARDFLOW_FLOW_MODE=crewai
ONBOARDFLOW_CREWAI_AMP_TRACING=true
GEMINI_API_KEY=<required for live Gemini calls>
```

Operational prerequisite:

```text
uv run crewai login
uv run crewai traces enable
uv run crewai traces status
```

Runtime note:

- Backend startup only confirms configuration. A trace is published after a real live CrewAI generation or refinement executes `Crew.kickoff()`.

Validation evidence:

```text
uv run pytest
uv run python -m compileall src tests
```

Results:

- Backend tests passed: 3 files, 12 tests.
- Compileall passed for `src` and `tests`.
- Existing warning remains from Starlette/FastAPI TestClient requesting future `httpx2`; unrelated to AMP tracing.
